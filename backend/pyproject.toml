[project]
name = "valte-backend"
version = "0.1.0"
description = "Valte HackSpain 2026 backend — crisis coordination API."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic-settings>=2.5",
    "supabase>=2.9",
    "httpx>=0.27",
]

